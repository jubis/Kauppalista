<ul id="items">
<% @items.each do |item| %>
	<li data-id="<%= item.id %>">
		<input class="check" type="checkbox">
		<%= item.name %><%= item.amount.strip != "" ? ', ' + item.amount : '' %>
	</li>
<% end %>
</ul>